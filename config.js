// Bonus Scout community counter configuration
// Create a free Supabase project, then paste your Project URL and anon public key below.
// It is safe for the browser to use the anon key when Row Level Security policies are configured
// exactly as described in SUPABASE_SETUP.md.
window.BONUS_SCOUT_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
